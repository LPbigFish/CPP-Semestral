# TECHNICAL STUFF I FOUND ONLINE

- `std::optional<T>` = `Option<T>` v Rustu.

- `std::expected<T, E>` = `Result<T, E>` v Rustu.

- Somehow C++ má pipe operator: `|>` v Elixiru, `|` a std::views funkce.
